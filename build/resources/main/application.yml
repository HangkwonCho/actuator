management:
  info:
    java:
      enabled: true
    os:
      enabled: true
    env:
      enabled: true
    git:
      mode: "full"
#  endpoint:
#    shutdown:
#    enabled: true
  endpoint:
    health:
      show-details: always
#      show-components: always
  endpoints:
    web:
      exposure:
        include: "*"
#        include: "health,info"
#        exclude: "env,beans"

info:
  app:
    name: hello-actuator
    company: yh

logging:
  level:
    hello.controller: debug